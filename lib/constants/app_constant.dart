import 'package:cambodia_geography/exports/exports.dart';

class AppContant {
  AppContant._internal();
  static const List<String> placeType = [
    "place",
    "restaurant",
  ];

  static const Locale km = Locale("km");
  static const Locale en = Locale("en");
}
